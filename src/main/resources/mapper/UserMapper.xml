<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ilovesshan.interceptor.mapper.UserMapper">

    <sql id="allColumns">
        user_id,
        user_id,
        username,
        nick_name,
        email,
        phonenumber,
        sex,
        avatar,
        password,
        status,
        del_flag,
        login_ip,
        login_date,
        create_by,
        create_time,
        update_by,
        update_time
    </sql>

    <select id="selectUserByName" resultType="ydlUser">
        select
            <include refid="allColumns"/>
        from
            ydl_user
        where
            `username` like #{username}
    </select>

</mapper>